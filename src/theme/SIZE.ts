import {moderateScale} from '@app/utils/scaling_unit';

const SIZE = {
  // Global sizes
  base: moderateScale(8),
  font: moderateScale(14),
  radius: moderateScale(8),
  radius_md: moderateScale(12),
  padding: moderateScale(16),
};
export default SIZE;
